const xyz = (x, y) => x + y;
export default xyz;

export const a = 10;
export const multiply = (x, y) => x + y;
export const root = (x) => Math.sqrt(x);

const _private = 'Private ';
export const publicName = (name) => _private + name;
